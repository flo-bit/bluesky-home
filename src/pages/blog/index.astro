---
import Layout from "../../layouts/Layout.astro";
import DesktopNavigation from "../../components/Navigation/DektopNavigation.astro";
import { getCollection } from "astro:content";
import BlogEntry from "../../components/BlogEntry.astro";

const blog = await getCollection("blog");
console.log(blog);

for (const post of blog) {
  console.log(post.data.value.title);
}
---

<Layout>
  <DesktopNavigation items={["about", "links", "feed", "blog"]} active="blog" />

  <div class="flex w-full flex-col max-w-2xl px-4 mt-8">
    {blog.map((post) => <BlogEntry post={post.data.value} />)}
  </div>
</Layout>
