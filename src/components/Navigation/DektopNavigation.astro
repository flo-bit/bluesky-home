---
import type { Item } from "./items";
import NavigationItem from "./NavigationItem.svelte";

let classes = "";

let { items, active } = Astro.props;
---

<nav class="mt-8 sm:mt-0">
  <ul
    class="flex rounded-full px-3 text-sm font-medium text-base-800 dark:text-base-200"
  >
    {
      items.map((item: Item) => (
        <NavigationItem
          href={`/bluesky-home/${item === "about" ? "" : item}`}
          isActive={active == item}
        >
          {item}
        </NavigationItem>
      ))
    }
  </ul>
</nav>
